<settings>
  <servers>
    <server>
      <id>nexus</id>
      <username>${env.NEXUS_CREDENTIALS_USR}</username>
      <password>${env.NEXUS_CREDENTIALS_PSW}</password>
    </server>
  </servers>
</settings>
